{
  "name": "neural-link",
  "version": "1.0.0",
  "description": "Cyberpunk Text Adventure",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "dotenv": "^17.3.1"
  }
}